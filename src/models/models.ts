export interface HeaderProps {
  height: number;
  url: string;
  component: React.ReactNode | React.ReactNode[];
}

export interface BtnProps {
  text: string;
  link: string;
  color: string;
  padding: string;
  size: string;
}
